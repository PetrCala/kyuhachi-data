# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# uv lockfile (skills run tests via `uv run`; the repo installs via pip/venv —
# this is a regenerable artifact, not a tracked manifest)
uv.lock

# macOS
.DS_Store

# Generated diff reports (regenerable; the snapshot DB is the tracked baseline)
.claude/skills/catalog-diff/reports/
reports/

# Staging scrape written by catalog-sync `detect`, consumed by `promote`
# (transient; the promoted snapshot.db is the tracked baseline)
data/snapshot.next.json

# Rendered HTML reports (render_html.py) and the Pages site built from them
# (build_site.py). A pure rendering of the committed Markdown, so the generators
# and the stylesheet are tracked; CI rebuilds the output on every push.
route_planning/html/
route_planning/site/
